# Compatibility objects

Objects in this folder are here to create an OpenAPI spec that is compatible with TRex conventions

E.g. TRex presupones OAuth and bearer tokens, which we will not have in the MVP
But we introduce a fake endpoint with OAuth support for this